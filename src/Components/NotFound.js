const NotFound = () => {
    return (
        <div style={myStyle}>
            <h1 style={{ color: "#999" }}>404</h1>
            <div style={{ color: "#999" }}>Page Not Found</div>
        </div>
    );
};

const myStyle = {
    textAlign: "center",
    fontSize: "xx-large",
};

export default NotFound;
